//= require lib/support

jQuery(function () {
  if (typeof RDPRO != "object") {
      window.RDPRO = {};
  }
  
  RDPRO["ui-roisummary"] = {
      name: "ui-roisummary"
  };

  console.log ("Intializing ROI Metrics");
});